import React from 'react';

const Output1 = ({ name, email, id }) => {
  return (
    <>
        <p>heart disease = 1 </p>
        <p>not heart disease = 0 </p>
        <h2>{this.state.pred}</h2>
        <button onClick = {() => {this.setState({status : "Input"})}} >back </button>
    </>

  );
}

export default Card;